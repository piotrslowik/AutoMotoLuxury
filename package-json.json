{
  "name": "auto_moto_luxury",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "frontend": "cd Frontend && npm run start",
    "backend": "cd Backend && npm run start",
    "heroku-postbuild": "cd Frontend && npm install && npm run build"
  }
}